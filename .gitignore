# secrets
.env
.env.local
web/.env.local

# foundry
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/lib/

# node
node_modules/
web/.next/
web/out/
web/next-env.d.ts
*.tsbuildinfo

# NOTE: web/src/lib/abis.ts and deployments.generated.ts are generated by `make abi` but are
# deliberately COMMITTED, so a fresh clone typechecks and builds without needing Foundry first.

# os
.DS_Store
*.log
